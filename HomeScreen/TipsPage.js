import React, { useState, useEffect, useContext } from 'react';
import { TextInput, View, SafeAreaView, Dimensions, FlatList, StyleSheet } from 'react-native'
import { Card, List, Text, Button, Icon } from '@ui-kitten/components';


const EditIcon = (props) => (
    <Icon {...props} name='edit-2'/>
  );

  const PlayIcon = (props) => (
    <Icon {...props} name='play-circle'/>
  );

// Select a study mode - The beginners study session is only 25 minutes

// Remove all distractions, during this period your only focus should be on studying

// Start the timer

// When the timer is up - Add a Note - Without referecing the material. Type out everything that your remember learning about

// Take a break - Breaks are nessecary when studying - they let our brains process and absorb information. 



function TipsPage(){


    return (
       

        <View style={{flex: 1}}>
    
    

      </View>
      
      
      );

    }



export default TipsPage

